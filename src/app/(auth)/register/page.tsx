import Header from "@/app/ui/reusable/header";
import RegisterForm from "@/app/ui/unique/registerForm";

export default function Home() {
    return (
      <main>
        <Header variant="login" />
        <RegisterForm />
      </main>
    );
  }
  